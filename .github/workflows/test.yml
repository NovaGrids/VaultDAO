name: CI

on:
  push:
    branches: [main, "feature/**", "fix/**"]
  pull_request:
    branches: [main]

jobs:
  contract-tests:
    name: Smart Contract Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: contracts/vault
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            contracts/vault/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('contracts/vault/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Lint (clippy)
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo test

      - name: Build WASM
        run: cargo build --target wasm32-unknown-unknown --release

      - name: Check WASM size against Soroban limit
        run: |
          LIMIT=262144
          WASM_PATH="target/wasm32-unknown-unknown/release/vault_dao.wasm"
          SIZE=$(stat -c%s "$WASM_PATH")
          SIZE_KB=$((SIZE / 1024))
          echo "WASM size: $SIZE bytes (${SIZE_KB} KB)"
          if [ "$SIZE" -gt "$LIMIT" ]; then
            echo "::error::WASM size $SIZE bytes exceeds Soroban limit of $LIMIT bytes (256 KB)"
            exit 1
          else
            echo "WASM size OK: $SIZE / $LIMIT bytes"
          fi

  backend-checks:
    name: Backend Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: backend/package-lock.json

      - name: Install root dependencies
        run: npm install

      - name: Install backend dependencies
        run: npm --prefix backend install

      - name: Copy backend env
        run: cp backend/.env.example backend/.env

      - name: Typecheck backend
        run: npm run backend:typecheck

      - name: Test backend
        run: npm run backend:test
